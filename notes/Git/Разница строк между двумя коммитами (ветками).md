Чтобы посчитать количество добавленных строк между двумя коммитами в Git, можно использовать команду `git diff` с параметрами `--stat` или `--shortstat`, либо более детальный анализ с `git log` и `git show`.

`git diff <commit1> <commit2> --stat
`git diff <branch1> <branch2> --stat``