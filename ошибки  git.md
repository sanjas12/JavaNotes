fatal: .git/index: index file smaller than expected

Эта ошибка означает, что **индексный файл Git (`.git/index`) повреждён** — его размер меньше ожидаемого. Обычно это происходит из-за:


rm .git/index
git reset